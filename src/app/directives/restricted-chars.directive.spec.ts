import { RestrictedCharsDirective } from './restricted-chars.directive';

describe('RestrictedCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedCharsDirective();
    expect(directive).toBeTruthy();
  });
});
