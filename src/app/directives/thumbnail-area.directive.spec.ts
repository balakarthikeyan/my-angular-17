import { ThumbnailAreaDirective } from './thumbnail-area.directive';

describe('ThumbnailAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbnailAreaDirective();
    expect(directive).toBeTruthy();
  });
});
